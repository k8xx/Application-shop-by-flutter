import 'package:flutter/material.dart';
import 'package:shop1/providers/orders.dart' as ord;

class OrderItem extends StatelessWidget {
  final ord.OrderItem order;
  const OrderItem(
      this.order,
      );

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}